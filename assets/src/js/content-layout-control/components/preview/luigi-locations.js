( function( $ ) {

	var clc = wp.customize.ContentLayoutControl;

	/**
	 * View class for the locations layout
	 *
	 * @augments wp.customize.ContentLayoutControl.Views.BaseComponentPreview
	 * @augments wp.Backbone.View
	 * @since 0.1
	 */
	clc.Views.component_previews['luigi-locations'] = clc.Views.BaseComponentPreview.extend();

} )( jQuery );
